# ROS2 / colcon
build/
install/
log/
.colcon/                 # colcon 메타/캐시

# ROS 런타임 로그(사용한다면)
.ros/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
*.pytest_cache/

# VS Code / IDE
.vscode/
*.code-workspace
.idea/
*.iml

# CMake / Tooling
CMakeLists.txt.user
compile_commands.json

# OS 잡파일
.DS_Store
Thumbs.db

# 기타(선택)
*.swp
*.swo

# ==========================
# ROS 2 Bag Logs (데이터 로그 제외)
# ==========================
# 1. 우리가 만든 로그 저장소 폴더 통째로 무시
bag_logs/

# 2. 실수로 다른 곳에 저장한 mcap 파일들도 무시
**/*.mcap
**/*.db3

# 3. ros2 bag record가 자동으로 생성하는 폴더명 패턴 무시
rosbag2*/

# ==========================
# SLAM Map Data (지도 데이터 제외)
# ==========================
# 1. 지도 이미지 파일 (바이너리라 용량이 큼) - 전체 무시
*.pgm

# 2. maps 폴더 안에 있는 모든 파일 무시
maps/*